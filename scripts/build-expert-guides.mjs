#!/usr/bin/env node
/**
 * Compiles scripts/expert-data/*.mjs into src/data/expert/guides.ts
 */
import fs from "fs";
import path from "path";
import { fileURLToPath } from "url";
import { expertData } from "./expert-data/index.mjs";

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const outPath = path.join(__dirname, "../src/data/expert/guides.ts");

const header = `// Auto-generated by scripts/build-expert-guides.mjs — do not edit by hand.
import type { ExpertGuideMap } from "./types";

export const expertBySlug: ExpertGuideMap = `;

fs.writeFileSync(outPath, header + JSON.stringify(expertData, null, 2) + " as ExpertGuideMap;\n");
console.log(`Wrote ${Object.keys(expertData).length} expert guides to ${outPath}`);
